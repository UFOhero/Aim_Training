# 仿 AimLab FPS 练枪模拟器 — 完整技术方案

> 依据《aimlab设计思路.doc》需求编写　|　版本 v1.0　|　日期 2026-08-07

---

## 一、项目概述

### 1.1 项目目标

开发一款**仿照 AimLab 的 Windows 桌面单机 FPS 枪法练习小游戏**，以 3D 第一人称视角呈现，让玩家通过点击屏幕上的小球进行枪法训练。画面保持简约，但核心点球功能必须完善、手感必须可靠。

### 1.2 需求要点梳理（源自设计文档）

| 编号 | 需求 | 说明 |
|------|------|------|
| R1 | 枪械贴图 | 手持枪械贴图即可，**不需要开枪动画，不需要后坐力系统** |
| R2 | 准星设置 | 可自由调整准星的**样式、大小、颜色** |
| R3 | 灵敏度设置 | 灵敏度换算**仿照 CS2**，保证肌肉记忆可迁移 |
| R4 | 练枪模式 | 屏幕常驻 **3 个小球**，打掉一个随机补充一个，始终同时存在 3 个 |
| R5 | 三种玩法模式 | ① 自由练习（无时间限制）② 计时模式（1/0.5/2/5 分钟，命中得分、连续命中额外加分、未命中中断连击）③ 计数模式（打满 30/50/100 个球的用时） |
| R6 | 画面风格 | 简约即可，核心交互完整 |

---

## 二、技术选型

### 2.1 推荐方案：Unity 6（LTS）+ C#

Unity 是该需求场景下**综合成本最低、上手最快、生态最完整**的选择。

| 考量维度 | 说明 |
|----------|------|
| 3D 第一人称 | 内置 Character Controller、Camera、PhysX，搭建 FPS 视角零成本 |
| 鼠标输入 | `Input.GetAxisRaw("Mouse X/Y")` 直接读取鼠标增量，方便实现 CS2 式灵敏度换算 |
| 枪械视角模型 | 通过 UI 层渲染枪械贴图（`Sprite` 或 `RawImage`），或挂一个「下挂相机」渲染枪械模型，均很简单 |
| 准星定制 | 用 `RawImage` + 动态颜色/尺寸即可，或用 `CanvasRenderer` 自绘 |
| 打包发布 | 一键导出 Windows EXE，无需额外配置 |
| 学习成本 | 开发者只需 C# 一种语言，无编译/依赖地狱 |

> **注意**：Unity 6 LTS 免费版（Personal）完全满足本项目需求，无需授权费用。

### 2.2 替代方案对比

| 方案 | 优点 | 缺点 | 结论 |
|------|------|------|------|
| **Godot 4**（GDScript/C#） | 免费开源、体积小、启动快 | FPS 模板生态较弱，需手写更多底层 | 备选，若团队更熟悉 Godot 可切换 |
| **Unreal Engine 5**（C++/蓝图） | 画面最强、自带 FPS 模板 | 体量大、编译慢、对简单 2.5D 点球项目是杀鸡用牛刀 | 不推荐 |
| **Three.js + Electron** | 跨平台、Web 技术 | 鼠标捕捉、性能调优、EXE 体积均不如 Unity 顺手 | 若以后想兼顾网页版可考虑，但本次为 Windows 桌面，不推荐 |

**结论：采用 Unity 6（LTS）+ C#，配合 URP（轻量渲染管线）保证画面简约且性能开销小。**

### 2.3 开发工具与环境

| 工具 | 用途 |
|------|------|
| Unity 6 LTS + VS Code | 主引擎与代码编辑器（本方案采用 VS Code 编写 C#，非 Visual Studio） |
| Git + 本地仓库 | 版本管理 |
| Blender（可选） | 如需自制 3D 小球/场景道具（默认用 Unity 内置 Sphere 即可） |
| 免费素材来源 | Unity Asset Store 免费枪械贴图包 / itch.io 免费素材 / 自绘低多边形枪械 |

---

### 2.4 VS Code + C# 开发环境搭建指南

> 本方案使用 **VS Code + C# 扩展** 作为日常编码环境。下面是零基础到可写代码的完整步骤。

#### 2.4.1 安装清单（共 3 项）

| 步骤 | 安装内容 | 类型 | 是否必装 |
|------|----------|------|----------|
| ① | .NET SDK（8 或 9 LTS） | 系统级运行时/编译器 | **必装** |
| ② | C# Dev Kit 扩展（含基础 C# 扩展） | VS Code 扩展 | **必装** |
| ③ | Unity 扩展（Unity Technologies 官方） | VS Code 扩展 | 配合 Unity 必装 |

> 说明：VS Code 本身不是"编译器"，C# 代码的编译、提示、调试全部依赖 .NET SDK。**顺序必须是：先装 SDK → 再装扩展 → 最后配置 Unity。**

#### 2.4.2 ① 安装 .NET SDK

1. 打开微软官网 <https://dotnet.microsoft.com/download>，选择 **.NET 8 或 .NET 9 LTS** 的 Windows x64 安装包下载。
2. 双击安装，全程默认下一步即可。
3. 验证安装：打开任意终端（PowerShell / CMD），输入：
   ```bash
   dotnet --version
   ```
   能打印出版本号（如 `9.0.x`）即安装成功。

#### 2.4.3 ② 安装 VS Code 扩展

打开 VS Code → 左侧扩展商店（Ctrl+Shift+X）→ 搜索并安装：

1. **C# Dev Kit**（微软官方，图标是 C# 徽标）
   - 这是「全家桶」：包含代码提示（IntelliSense）、调试、项目管理、测试面板。
   - 安装时会自动连带安装基础扩展 **C# for Visual Studio Code**，无需重复手动安装。
2. **Unity**（发行方：Unity Technologies）
   - 提供：一键启动/停止 Unity Play 模式、调试器挂载、Unity 专属提示。
   - 装了它才能直接在 VS Code 里断点调试 Unity 脚本。

> 若不需要断点调试，可只装 C# Dev Kit；但推荐两者都装，调试练枪手感问题时几乎必需。

#### 2.4.4 ③ 配置 Unity 使用 VS Code

安装完成后还需让 Unity 知道"用 VS Code 打开脚本"：

1. 打开 Unity → 菜单栏 `Edit` → `Preferences`（偏好设置）。
2. 左侧选 **External Tools**（外部工具）→ **External Script Editor** 下拉框选择 **Visual Studio Code**。
3. 若下拉框里没有 VS Code，点击 `Browse...` 手动定位到 `Code.exe`（通常位于 `C:\Users\<用户名>\AppData\Local\Programs\Microsoft VS Code\Code.exe`）。

#### 2.4.5 首次打开工程时的注意事项

- 用 VS Code 打开 **Unity 项目文件夹**（含 `Assets/` 的那个根目录），**不是** 单个脚本文件。
- 首次加载 Unity 生成的项目文件（`.csproj`）可能需要几十秒，VS Code 右下角可能弹出「是否安装缺失依赖」，点 **Yes / Install** 等待完成。
- 若代码提示（IntelliSense）不生效，按 `Ctrl+Shift+P` → 输入 `Reload Window` 回车重载窗口，或等待 Unity 重新生成 `.csproj` 后再打开。

#### 2.4.6 常见问题排查

| 现象 | 原因 | 解决 |
|------|------|------|
| `dotnet --version` 无输出 | SDK 未安装或未加入 PATH | 重装 SDK，装完重启终端 |
| 代码无提示/波浪线不消失 | Unity 未生成 .csproj 或扩展未加载完 | 等 Unity 编译完成；重载 VS Code 窗口 |
| VS Code 打开脚本报"缺 OmniSharp" | C# 扩展依赖未装 | 安装 C# Dev Kit，它会自动处理依赖 |
| 点击 Unity 脚本不跳转 VS Code | 外部脚本编辑器未配置 | 按 2.4.4 重新配置 External Script Editor |

---

## 三、总体架构

### 3.1 系统模块划分

```
┌─────────────────────────────────────────────────────┐
│                     Unity 场景                        │
│                                                     │
│  ┌──────────────┐   ┌──────────────┐               │
│  │  输入系统     │   │  模式管理器   │               │
│  │ InputReader  │   │ ModeManager  │               │
│  └──────┬───────┘   └──────┬───────┘               │
│         │                  │                       │
│  ┌──────▼───────┐   ┌──────▼───────┐               │
│  │ 玩家控制器    │   │  靶球系统     │               │
│  │ PlayerController│ │  TargetManager│              │
│  │ (视角旋转/灵敏度)│ │ (3球常驻/补球) │              │
│  └──────┬───────┘   └──────┬───────┘               │
│         │                  │                       │
│  ┌──────▼───────┐   ┌──────▼───────┐               │
│  │ 准星系统      │   │  计分系统     │               │
│  │ CrosshairUI  │   │ ScoreManager  │               │
│  └──────────────┘   └──────────────┘               │
│                                                     │
│  ┌──────────────┐   ┌──────────────┐               │
│  │ 枪械视角层    │   │ 设置与存档     │               │
│  │ WeaponViewModel│ │ Settings/PlayerPrefs│          │
│  └──────────────┘   └──────────────┘               │
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │              UI 框架（Unity UGUI）            │   │
│  │  主菜单 / 模式选择 / 设置面板 / 结算面板 / HUD  │   │
│  └─────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘
```

### 3.2 项目目录结构（规划）

```
Assets/
├── Scenes/                 # 场景
│   ├── MainMenu.unity
│   └── Training.unity
├── Scripts/
│   ├── Input/              # InputReader, SensitivityConfig
│   ├── Player/             # PlayerController, CameraController
│   ├── Crosshair/          # CrosshairManager
│   ├── Weapon/             # WeaponViewModel
│   ├── Targets/            # TargetManager, TargetBall
│   ├── GameModes/          # ModeBase, FreeMode, TimedMode, CountMode
│   ├── Scoring/            # ScoreManager, ComboManager
│   ├── UI/                 # 各类面板控制脚本
│   └── Settings/           # SettingsData, SettingsPersist
├── Prefabs/
│   ├── TargetBall.prefab
│   └── WeaponViewModel.prefab
├── UI/                     # 图集、准星贴图、字体
├── Textures/               # 枪械贴图、场景贴图
└── Resources/              # 可运行时加载的配置（可选）
```

### 3.3 关键设计原则

1. **单场景训练**：主菜单和训练用两个场景，训练场景内所有系统解耦，便于单独测试。
2. **事件驱动**：命中、补球、模式结束等通过 C# 事件/UnityEvent 广播，避免系统间互相引用。
3. **配置化**：灵敏度、准星、模式参数（小球数量=3、生成半径、球大小、计时档位）全部集中到配置类，便于调整。

---

## 四、核心模块详细设计

### 4.1 输入与灵敏度系统（R3，重点）

#### 4.1.1 CS2 灵敏度机制说明

CS2（Source 引擎）的鼠标灵敏度模型：

- 玩家设置一个灵敏度值 `Sensitivity`（如 2.0）。
- 引擎将鼠标原始位移 `counts`（像素移动量）乘以固定角速度系数 `yaw = 0.022°/count`。
- 公式：**视角旋转角度（度） = Sensitivity × 0.022 × 鼠标移动点数**。
- 由此可换算 **360° 移动距离（cm/360）**：

```
cm/360 = (360 ÷ (Sensitivity × 0.022 × DPI)) × 2.54
```

示例：400 DPI、灵敏度 2.0，则
cm/360 = (360 ÷ (2.0 × 0.022 × 400)) × 2.54 ≈ 51.95 cm。

#### 4.1.2 实现方案

- 在设置面板中，让玩家输入 **CS2 灵敏度数值**和**鼠标 DPI**（DPI 可选，仅用于展示 cm/360 参考值）。
- 代码中禁止使用 Unity 的 `Mouse X`/`Mouse Y` 的平滑处理，改用 **原始增量**：
  ```csharp
  float dx = Input.GetAxisRaw("Mouse X");
  float dy = Input.GetAxisRaw("Mouse Y");
  // 注意 GetAxisRaw 已按帧返回带符号的计数增量（默认灵敏度1下为每帧鼠标点数）
  ```
  再乘以换算系数：`yawDegrees = dx * Sensitivity * 0.022f`。
- 提供「**原始输入（Raw Input）**」选项（Windows 下启用后更精确，避免系统指针加速），推荐开启。
- 提供「**显示器距离 / cm360 参考值**」显示，方便玩家与 CS2 中对齐。

> 验收口径：在 CS2 中调整到某一灵敏度后，在练枪器中设置相同灵敏度，同样的鼠标甩动手感旋转相同角度。

#### 4.1.3 俯仰角限制

- 俯仰（Yaw 绕 Y 轴）不限；**俯仰角（Pitch）限制在 ±89°**，避免镜头翻转，与 FPS 常规一致。

---

### 4.2 准星系统（R2）

- 以 **UGUI Canvas（Screen Space - Overlay）** 实现，保证准星始终在最上层、不受 3D 遮挡。
- 准星结构：由若干**矩形贴图块**（上/下/左/右，可选中心点）组合而成。

| 可调参数 | 范围建议 | 说明 |
|----------|----------|------|
| 样式 | 十字 / 点 / 圆环 / 仅中心点 | 至少实现 十字 与 点 两种，其余可后补 |
| 大小 | 长度与间距 0~100px | 滑块控制 |
| 颜色 | RGB + 透明度 | ColorPicker 或预设色板 |
| 中心点 | 开/关、大小 | 辅助瞄准 |

- 所有参数通过 `CrosshairSettings` 数据类保存，修改即时生效（OnValueChanged 实时刷新）。
- 追加可选：动态扩散（命中时放大）——默认关闭，作为后续扩展，不阻塞当前版本。

---

### 4.3 枪械视角模型（R1）

- **方案 A（推荐）**：枪械作为 3D 模型挂在主相机下（跟随相机），作为「视角模型（View Model）」。
  - 在玩家身上挂「枪械挂点」，模型仅渲染在主相机层（Layer 分离），防止穿模时被世界物体遮挡。
  - 移动时叠加轻微的手持摆动（可选用 Perlin 噪声），带来手持感。
- **方案 B**：枪械用 2D 贴图显示在屏幕右下角（`RawImage`），实现最简。
- 本项目按 R1 要求：**不实现开枪动画、不实现后坐力**，扣动鼠标左键 = 直接判定命中。
- 素材：从 Asset Store 免费枪械模型/贴图包获取（如 "Low Poly Gun"），或用一张俯视角枪械贴图即可。

---

### 4.4 靶球系统（R4）

#### 4.4.1 常驻 3 球逻辑

```
TargetManager:
  onStart   → 生成 3 个小球（随机位置）
  onHit     → 销毁被击中球 → 立即生成 1 个新球 → 保证场上始终 3 个
```

- 每个球是一个 `TargetBall` 预制体（Unity 内置 Sphere + 自发光材质）。
- 生成范围：在玩家视野前方一定扇形/球壳区域内随机（例如：距玩家 8~20 米、水平 ±45°、垂直 ±25°，且不与已有球重叠、不生成在准星正中央附近，避免「开局白送」）。
- 球体大小可配置（默认半径 0.25m，可随难度调整）。
- 球体可选轻微浮动动画（正弦上下浮动），增强可瞄性（后期扩展）。

#### 4.4.2 命中判定

- 使用**射线检测（Raycast）**：点击时从相机中心向准星方向发射射线，命中球体则计为命中。
- 命中反馈：球体播放 0.1~0.2s 的缩放/消散动画后销毁，并播放命中音效（可选）。
- 未命中：无命中反馈（不生成任何东西），按当前模式规则处理。

---

### 4.5 三种练枪模式（R5）

统一由 `ModeBase` 抽象类管理生命周期：

```csharp
abstract class ModeBase {
    void Start();
    void OnHit();
    void OnMiss();
    bool IsEnded { get; }
    void End();
}
```

| 模式 | 规则 | 计分 | 结束条件 |
|------|------|------|----------|
| **自由练习** `FreeMode` | 无时间限制，无限打 | 显示累计命中数与命中率（可选） | 玩家主动退出 |
| **计时模式** `TimedMode` | 可选择 **0.5 / 1 / 2 / 5 分钟** | 每次命中 +1 分；**连续命中额外 +1（连击 ≥2 起）**；未命中不加分且**连击清零** | 倒计时归零，弹出结算面板 |
| **计数模式** `CountMode` | 目标数量可选 **30 / 50 / 100 个** | 记录总用时，可选显示平均命中间隔 | 打满目标数，弹出结算面板 |

#### 4.5.1 连击系统（计时模式核心）

- `ComboManager` 维护 `currentCombo` 与 `maxCombo`。
- 命中：`currentCombo++`；若 `currentCombo >= 2`，本球得分 = `1 + 1`（基础分 + 连击奖励）。
- 未命中：`currentCombo = 0`，本球不得分。
- HUD 实时显示：当前得分 / 连击数 / 最高连击 / 剩余时间。

#### 4.5.2 模式选择与参数

- 主菜单 → 模式选择 → 按模式弹出参数面板（计时选时长、计数选数量）→ 进入训练场景。

---

### 4.6 UI 与设置面板

| 面板 | 内容 |
|------|------|
| 主菜单 | 游戏标题、模式选择入口、设置入口、退出 |
| 模式选择 | 自由练习 / 计时（选时长）/ 计数（选数量） |
| 设置面板 | 灵敏度（CS2 数值）、DPI、原始输入开关、cm360 参考值、准星样式/大小/颜色、音量（可选） |
| 训练 HUD | 准星、当前得分、连击、剩余时间/剩余球数、暂停按钮 |
| 结算面板 | 本次成绩：得分 / 命中率 / 最高连击 / 总用时 / 平均用时，以及「再来一次 / 返回主菜单」 |

- 设置数据持久化用 `PlayerPrefs`（或轻量 JSON 存档），重启后保留。

---

### 4.7 数据统计与存档

- 每局结束记录：模式、参数、得分、命中数、未命中数、命中率、最高连击、总用时。
- 版本 v1.0 只做**本地简单存档**（最近 N 局历史，PlayerPrefs 存 JSON）。
- 排行榜/长期统计留待后续版本。

---

## 五、分阶段实施计划

> 以下阶段建议按顺序推进，每阶段结束都有**可运行、可验证**的产出。总工期参考见第八节。

### 阶段 0：环境搭建与项目初始化

**任务清单**
1. 安装 Unity 6 LTS + Visual Studio 2022（含 .NET 与 C# 工作负载）。
2. 新建 Unity 3D 项目，模板选 **Universal 3D（URP）**。
3. 初始化 Git 仓库，配置 `.gitignore`（Unity 官方模板）。
4. 搭建基础目录结构（Scenes / Scripts / Prefabs / UI / Textures）。
5. 建立两个空场景：MainMenu、Training，并加入 Build Settings。

**产出**：可打开的空 Unity 工程，场景可切换。
**验收标准**：工程无报错，从 MainMenu 能进入 Training 场景。

---

### 阶段 1：第一人称控制与灵敏度系统（R3）

**任务清单**
1. 实现 `PlayerController`：相机跟随、鼠标视角旋转。
2. 实现 CS2 灵敏度换算模块 `SensitivityConfig`（0.022° 系数、cm360 计算）。
3. 接入原始鼠标输入，剔除引擎平滑。
4. 俯仰角限制 ±89°。
5. 实现最小可用的灵敏度设置 UI（输入灵敏度值 → 立即生效）。

**产出**：进入场景后可用鼠标自由环视，灵敏度数值与 CS2 对齐。
**验收标准**：在 CS2 与练枪器设置相同灵敏度，鼠标移动相同距离，画面旋转角度一致；俯仰无法翻转。

---

### 阶段 2：准星系统（R2）

**任务清单**
1. 搭建 UGUI Canvas 准星（十字/点两种样式的基础结构）。
2. 实现 `CrosshairManager`：样式、大小、颜色、中心点开关，参数即时生效。
3. 设置面板中接入准星设置（滑块 + 颜色选择）。
4. 准星参数持久化（PlayerPrefs）。

**产出**：设置面板可调准星样式/大小/颜色，重启后保留。
**验收标准**：所有准星参数调整即时反映到画面；重启游戏后设置不丢失。

---

### 阶段 3：枪械视角模型（R1）

**任务清单**
1. 获取/制作枪械模型或贴图（免费素材）。
2. 实现 `WeaponViewModel`：枪械挂载主相机，Layer 隔离，避免遮挡。
3. 可选：轻微手持摆动动画。
4. 确认无开枪动画、无后坐力逻辑。

**产出**：屏幕手持枪械可见，鼠标环视时枪械跟随。
**验收标准**：枪械正确显示且不穿模；移动/旋转时手持感自然。

---

### 阶段 4：靶球系统（R4）

**任务清单**
1. 制作 `TargetBall` 预制体（Sphere + 发光材质）。
2. 实现 `TargetManager`：开局生成 3 球、命中后随机补球、保证恒为 3 球。
3. 实现生成规则（前方扇形随机、不与现有球重叠、不过近不过偏）。
4. 实现射线命中判定与命中反馈（消散动画/音效）。
5. 增加命中计数基础 HUD（命中数 / 未命中数）。

**产出**：场上恒定 3 个小球，点击可打掉并补充。
**验收标准**：任意时刻场上恰有 3 球；命中判定准确（准星对准即中，未对准不中）；补球不生成在准星正中央。

---

### 阶段 5：三大练枪模式（R5）

**任务清单**
1. 实现 `ModeBase` 抽象基类与模式管理器 `ModeManager`。
2. 实现 **自由练习** `FreeMode`：无限打，仅统计。
3. 实现 **计时模式** `TimedMode`：
   - 档位：0.5 / 1 / 2 / 5 分钟；
   - 计分：命中 +1，连击 ≥2 起额外 +1；
   - 连击管理 `ComboManager`：未命中清零；
   - 倒计时 HUD。
4. 实现 **计数模式** `CountMode`：30 / 50 / 100 目标数，计时记录。
5. 主菜单 → 模式选择 → 参数选择 → 进入训练的完整流程。

**产出**：三个模式均可从主菜单进入并正常游玩。
**验收标准**：
- 自由练习无时间限制、可无限打；
- 计时模式倒计时正确，命中得分、连击加分、未命中断连击均符合规则，时间到弹出结算；
- 计数模式打满目标数自动结束并显示总用时。

---

### 阶段 6：UI 完善、结算与存档

**任务清单**
1. 完善主菜单、模式选择、设置面板、结算面板的美观度（简约风格）。
2. 结算面板：得分 / 命中率 / 最高连击 / 总用时 / 平均用时。
3. 每局成绩本地存档（PlayerPrefs + JSON）。
4. 暂停功能（Esc 暂停/继续）。
5. 音效与背景音乐（可选、轻量）。

**产出**：完整可玩的游戏闭环。
**验收标准**：一局结束后正确展示成绩；返回主菜单可再次开始；历史成绩可查看（可选）。

---

### 阶段 7：优化、测试与打包发布

**任务清单**
1. 性能检查：URP 渲染、GC 分配优化（避免每帧产生垃圾）、对象池（球体反复创建销毁改为对象池）。
2. 边界测试：极高/极低灵敏度、准星极端大小、窗口分辨率变化、DPI 变化。
3. 打包 Windows EXE（Build Settings → Windows x86_64）。
4. 真机（玩家电脑）冒烟测试：安装运行、鼠标输入正常。
5. 整理 README 与操作说明。

**产出**：可分发安装的 Windows 桌面版本。
**验收标准**：打包后在目标机器免编译直接运行；全部需求点 R1~R6 均通过测试。

---

## 六、整体验收标准（汇总）

| 需求 | 验收标准 |
|------|----------|
| R1 枪械贴图 | 手持枪械显示正确；无开枪动画、无后坐力 |
| R2 准星 | 样式/大小/颜色可调，即时生效，重启保留 |
| R3 灵敏度 | 与 CS2 换算一致，支持原始输入，俯仰受限 |
| R4 3 球常驻 | 任意时刻场上恰有 3 球，打掉立即补充 |
| R5 三种模式 | 自由/计时/计数规则完整，连击逻辑正确，结算正确 |
| R6 简约画面 | 画面整洁，核心点球流畅、判定准确（60 FPS） |

---

## 七、风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| 鼠标手感与 CS2 有偏差 | 灵敏度换算失真 | 尽早（阶段1）实现换算并对照测试；支持 DPI 输入做 cm360 校验 |
| 球体生成位置不合理（太偏/重叠/贴脸） | 体验差 | 制定严格随机规则 + 重叠检测（阶段4 统一处理） |
| 高频创建/销毁球体造成卡顿 | 掉帧 | 阶段7 引入对象池，球体复用 |
| 免费素材风格不统一 | 观感粗糙 | 统一采用 Low Poly / 单色简约风格 |
| 需求后续新增模式/武器 | 扩展困难 | 模式用抽象基类、武器用挂点系统，预留扩展点 |

---

## 八、里程碑与工期估算

> 单人全职开发参考估算，实际按熟练度浮动 ±30%。

| 阶段 | 内容 | 参考工期 |
|------|------|----------|
| 0 | 环境搭建 | 0.5 ~ 1 天 |
| 1 | 第一人称 + 灵敏度 | 1 ~ 2 天 |
| 2 | 准星系统 | 0.5 ~ 1 天 |
| 3 | 枪械视角 | 0.5 ~ 1 天 |
| 4 | 靶球系统 | 1 ~ 2 天 |
| 5 | 三大模式 | 1.5 ~ 3 天 |
| 6 | UI 结算与存档 | 1 ~ 2 天 |
| 7 | 优化打包发布 | 1 ~ 2 天 |
| **合计** | | **约 7 ~ 14 天** |

---

## 九、后续扩展方向（非本期）

- 更多练枪模式：Sixshot、Gridshot、追枪（Tracking）等。
- 更多枪械与切换。
- 球体大小/移动速度动态难度。
- 本地历史成绩统计与图表。
- 联网排行榜（需账号系统，暂不规划）。

---

*本方案基于《aimlab设计思路.doc》需求，采用 Unity 6 + C# 实现 Windows 桌面 3D 第一人称练枪模拟器。*
